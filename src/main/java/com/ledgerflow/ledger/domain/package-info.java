/**
 * Ledger module — domain layer.
 *
 * <p>Balance and posting rules for recording the effect of payments.
 * Must not depend on {@code application} or {@code infrastructure} packages
 * of any module, or on Spring/persistence/web frameworks.
 */
package com.ledgerflow.ledger.domain;
