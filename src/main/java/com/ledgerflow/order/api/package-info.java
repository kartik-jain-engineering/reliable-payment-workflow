/**
 * Order module — api layer.
 *
 * <p>The REST boundary: request/response models, the hand-written mapper
 * between them and the domain, the controller and the module-scoped
 * exception handler. Request models carry the structural (Bean Validation)
 * rules; business rules stay in {@code domain}.
 */
package com.ledgerflow.order.api;
