/**
 * Order module — domain layer.
 *
 * <p>The {@code Order} aggregate root, its value objects and the state
 * machine that governs order lifecycle. Must not depend on
 * {@code application} or {@code infrastructure} packages of any module,
 * or on Spring/persistence/web frameworks.
 */
package com.ledgerflow.order.domain;
