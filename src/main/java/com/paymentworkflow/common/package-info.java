/**
 * Shared kernel: cross-cutting types (base exceptions, value objects, utilities)
 * that every module is permitted to depend on. Kept intentionally small —
 * anything module-specific belongs in that module instead.
 */
package com.paymentworkflow.common;
