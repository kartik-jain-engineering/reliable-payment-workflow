/**
 * Payment module — application layer.
 *
 * <p>Use cases / orchestration that coordinate {@code domain} objects and
 * {@code infrastructure} ports. This is the module's entry point for other
 * modules to call into (once a public API is introduced).
 */
package com.paymentworkflow.payment.application;
