# Profile used by the test suite. The datasource itself is supplied at
# runtime by Testcontainers via @ServiceConnection — no URL is set here.
spring:
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate

logging:
  level:
    com.ledgerflow: DEBUG
    org.testcontainers: INFO
